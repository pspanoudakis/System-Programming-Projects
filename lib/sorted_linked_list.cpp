#include "../include/sorted_linked_list.hpp"

SortedLinkedList::SortedLinkedList(): LinkedList() {
    //this->head = new ListNode();
    //this->head->data = 0;
    //this->head->next = 0;
    //All seems to work fine
}

SortedLinkedList::~SortedLinkedList() {}
